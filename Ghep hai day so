void ghepHaiDs(daySo& a, daySo& b) {
	int temp = 0;
	int pt = 0;
	for (int i = laySoPT(a); i <= laySoPT(a) + (laySoPT(b) - 1); i++) {
		a.arr[i] = b.arr[temp];
		temp++;
		pt++;
	}
	a.x = laySoPT(a) + laySoPT(b);
}
